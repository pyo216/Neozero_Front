# Node modules cache (already included)
node_modules/.cache/

# Environment variables (already included)
.env

# Log files (already included)
*.log

# Build artifacts (already included)
dist/
build/
public/

# IDE settings (already included)
.idea/
.vscode/
.config/

# Temporary files and cache (already included)
npm-debug.log
yarn-error.log

# Test related files (already included)
coverage/
# tests/  # 必要に応じて

# Local settings (already included)
.gitconfig
.editorconfig

# macOS specific
.DS_Store
